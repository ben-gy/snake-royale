{
  "name": "snake-royale",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Classic snake, but 2–6 snakes share one arena — eat, grow, cut rivals off; last snake slithering wins.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "trystero": "^0.25.3"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
